# TODO
- [x] Inspect dashboard and component-loading logic.
- [x] Fix incorrect component paths in `user/Dasboard.html` (was using `../components/...`, now uses `./Dashboard-homepage-components/...`).
- [ ] Verify in browser console that dashboard renders components without 404 errors.
